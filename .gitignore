# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files (keep .env.example)
.env
.env.development
# Note: .env.production IS committed — it holds only public Cloud Run URLs (no
# secrets), so the CI build (npm run build) bakes the right backend into the
# bundle. See CLAUDE.md: "API URLs are public Cloud Run endpoints."

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
